// select all columns from listings rounded average cost per night of pricing table as avg_cost_per_night from listings l join listing_daily_prices p ON l.id = p.listing_id where

// select

// module.exports.getCoreData = knex.select('listings')
